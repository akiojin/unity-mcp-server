# package.jsonまたはpnpm-lock.yamlが変更されているか確認
if git diff-tree -r --name-only --no-commit-id ORIG_HEAD HEAD | grep -E '(package\.json|pnpm-lock\.yaml)'; then
  echo ""
  echo "⚠️  package.json or pnpm-lock.yaml has changed."
  echo "   Run 'pnpm install' to update dependencies."
  echo ""
fi
