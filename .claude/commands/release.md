---
description: developからmainへのリリースPRを作成します。
---

## ユーザー入力

```text
$ARGUMENTS
```

## 概要

このコマンドは、developブランチの変更をmainブランチにリリースするためのPRを作成します。

**処理フロー**:
1. developとmainの差分を確認
2. semantic-release dry-runでリリースノートプレビューを生成
3. ユーザーに確認プロンプトを表示
4. 確認後、develop → main PRを作成

## 実行手順

### 1. developとmainの差分確認

まず、developとmainの差分を確認します。

```bash
cd /unity-mcp-server && git fetch origin && git log origin/main..origin/develop --oneline
```

差分がない場合は、以下のメッセージを表示して終了:
```
ℹ️ developとmainに差分がありません。リリースする変更がないため、PR作成をスキップします。
```

### 2. semantic-release dry-run実行

developブランチをチェックアウトして、semantic-release dry-runを実行:

```bash
cd /unity-mcp-server && git checkout develop && git pull origin develop
cd mcp-server && npx semantic-release --dry-run --no-ci 2>&1 | grep -A 100 "The next release version"
```

出力例:
```
[semantic-release] › ℹ  The next release version is 2.17.0
[semantic-release] › ℹ  Release note for version 2.17.0:
## [2.17.0] (2025-11-07)

### Features
* Add video capture support
* Implement 3-tier release flow
```

### 3. リリースノートプレビュー表示

ユーザーに以下の情報を表示:

```markdown
## 🚀 リリース準備完了

**次のバージョン**: v2.17.0

**リリース内容**:
## [2.17.0] (2025-11-07)

### Features
* Add video capture support
* Implement 3-tier release flow

**リリース成果物**:
- ✅ MCPサーバー（npm） → npmjs.comに自動publish
- ✅ LSPサーバー（全プラットフォーム） → GitHub Releaseに添付
- ✅ Unity Package → バージョン同期
- ✅ CHANGELOG.md → 自動生成

**マージ後の自動フロー**:
1. semantic-release実行 → バージョンアップ＆タグ作成
2. GitHub Release作成
3. MCPサーバー npm publish
4. LSPサーバービルド（全プラットフォーム）

---

このリリースを実行しますか？ (yes/no)
```

### 4. ユーザー確認

ユーザーの入力を待機:
- **yes**: PR作成に進む
- **no**: キャンセルして終了

### 5. develop → main PR作成

ユーザーが承認した場合、PRを作成:

```bash
# PRタイトル: semantic-releaseの次バージョンを使用
PR_TITLE="release: v2.17.0"

# PRボディ
PR_BODY=$(cat <<EOF
## 🚀 リリース: v2.17.0

このPRは、developブランチの変更をmainブランチにリリースします。

---

## 📋 リリース内容

$(git log origin/main..origin/develop --oneline)

---

## 🎯 リリースノート

## [2.17.0] (2025-11-07)

### Features
* Add video capture support
* Implement 3-tier release flow

---

## 🤖 自動リリースフロー

このPRをマージすると、以下が自動的に実行されます：

1. **semantic-release**: package.json、Unity Package、CHANGELOG.mdを更新、タグ作成
2. **GitHub Release**: リリースノートと共にリリース作成
3. **npm publish**: MCPサーバーをnpmjs.comに公開
4. **LSPサーバービルド**: 全プラットフォーム（linux-x64, osx-x64, osx-arm64, win-x64）でビルド、GitHub Releaseに添付

---

⚠️ **重要**: このPRはRequiredチェック成功後に自動マージされます。

📝 **仕様**: specs/SPEC-1ac96646/spec.md を参照してください。
EOF
)

# PR作成（auto-merge無効）
gh pr create --base main --head develop --title "$PR_TITLE" --body "$PR_BODY"
```

### 6. 完了メッセージ

PR作成成功後、以下を表示:

```markdown
✅ リリースPRが作成されました！

**PR URL**: https://github.com/akiojin/unity-mcp-server/pull/XX

**次のステップ**:
1. PRのCI/CDチェック（Required checks）の完了を待機
2. チェック成功 → 自動的にmainへマージ
3. マージ後 → semantic-release自動実行でリリース開始

**マージ後の成果物**:
- npmjs.com: @akiojin/unity-mcp-server v2.17.0
- GitHub Release: v2.17.0（LSPバイナリ添付）
- CHANGELOG.md: 自動更新

🎉 リリース準備完了です！
```

## エラーハンドリング

### developとmainに差分がない
```
ℹ️  developとmainに差分がありません。
リリースする変更がないため、PR作成をスキップします。

developブランチに新しい機能をマージしてから、再度/releaseを実行してください。
```

### semantic-release dry-run失敗
```
❌ semantic-release dry-runが失敗しました。

原因:
- Conventional Commits規約に準拠していないコミットがある可能性
- package.jsonやnode_modulesに問題がある可能性

対策:
1. developブランチのコミットメッセージを確認
2. mcp-serverディレクトリで `npm ci` を実行
3. エラーログを確認: [表示されたエラー]
```

### gh CLI認証エラー
```
❌ GitHub CLI認証エラー

以下のコマンドで認証してください:
gh auth login
```

## ガイドライン

- ✅ リリース前に必ずリリースノートをプレビュー表示
- ✅ ユーザー確認を必須とする（自動実行しない）
- ✅ PRは手動マージを前提（auto-merge無効）
- ❌ mainへの直接pushは禁止（PRを経由）
