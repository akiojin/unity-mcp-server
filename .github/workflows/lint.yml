name: Lint

on:
  pull_request:
    branches: [develop, main]

jobs:
  lint:
    name: Markdown, ESLint & Formatting
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install build tools (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y build-essential python3 make gcc g++

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run Markdownlint
        run: >
          pnpm exec markdownlint "mcp-server/**/*.md"
          --config .markdownlint.json
          --ignore-path .markdownlintignore

      - name: Run ESLint
        run: pnpm exec eslint mcp-server/src mcp-server/tests --ext .js --max-warnings=0

      - name: Check formatting
        run: pnpm exec prettier --check mcp-server
        continue-on-error: true

  commitlint:
    name: Commit Message Lint
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Validate commit messages
        run: >
          pnpm exec commitlint
          --from ${{ github.event.pull_request.base.sha }}
          --to ${{ github.event.pull_request.head.sha }}
          --verbose
